{
  "name": "jaipur-ride-workspace",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "website"
  ],
  "scripts": {
    "sync": "node scripts/sync-assets.js",
    "dev:website": "npm run dev --workspace=website",
    "build:mobile": "npm run build --workspace=mobile",
    "build:website": "npm run build --workspace=website"
  }
}
